FROM python:3.11-slim

WORKDIR /app
COPY quarantine/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY quarantine/app.py .

EXPOSE 8090
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8090", "--workers", "2"]
